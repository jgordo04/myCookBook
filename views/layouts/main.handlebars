<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
    <meta charset="utf-8">
    <title>myCookBook</title>
  </head>
  <body>
    <!-- Header -->
    <nav class="navbar navbar-default navbar-fixed-top title">
      <ul class="nav navbar-nav navbar-right">
          {{#if user}}
              <li> <a href="/users/{{user.username}}"> Hello, {{user.username}} </a> </li>
              <li><a href="/logout">Log out</a></li>
          {{else}}
              <li><a href="/signup">Sign up</a></li>
          {{/if}}
      </ul>
      <h1>myCookBook</h1>
    <!-- </nav> -->
    <nav class="navbar navbar-default">
      {{#if user}}
      <a class="navbar-brand" href="/cookbook">Recipes</a>
      <a class="navbar-brand" href="/cookbook/new">Add recipe</a>
      {{/if}}
    </nav>
  </nav>
  <div class="body-class">
    <!-- Body -->
    {{{ body }}}
  </div>

    <!-- Footer -->
  </br>
  <nav class="navbar navbar-default navbar-fixed-bottom">
      <p class="navbar-text">Brought to you by Jamie's JS Class</p>
  </nav>
  </body>
</html>
